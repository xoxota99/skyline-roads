package com.skyline.roadsys.streetgraph.pattern;

import org.apache.commons.logging.*;
import org.junit.*;

import com.skyline.roadsys.streetgraph.*;

public class TestRasterRoadPattern {
	private Log logger = LogFactory.getLog(this.getClass());

	@Test
	public void testBasic()
	{
		RasterRoadPattern rp = new RasterRoadPattern();
		Path roadSegment;

//		roadSegment = rp.getNextIdealRoadSegment();
//		logger.debug(roadSegment.toString());

//		roadSegment = rp.getNextIdealRoadSegment();
//		logger.debug(roadSegment.toString());

	}
}
